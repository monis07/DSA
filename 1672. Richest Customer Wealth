class Solution {
    public int maximumWealth(int[][] accounts) {
        int max=0;
        int sum=0;
        for(int n[]:accounts){
            for(int i:n){
                sum+=i;
            }
            max=Math.max(max,sum);
            sum=0;
        }
       return max; 
    }
}
